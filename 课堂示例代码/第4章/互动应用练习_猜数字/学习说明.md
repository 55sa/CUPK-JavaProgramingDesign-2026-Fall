# 类与对象应用练习：猜数字
# Практическое задание: классы и объекты — «Угадай число»
# Classes and Objects Assignment: Guess the Number

姓名 / Имя / Name：__________　学号 / Номер студента / Student ID：__________

## 1. 你要做什么 / Что нужно сделать / What you need to do

**中文**

完成一个通过键盘输入进行交互的 Java 猜数字游戏。程序已提供基本框架，你需要填写 **24 个 TODO**，实现游戏对象的初始化、次数记录、答案判断和方法调用。建议用时 45–60 分钟。

电脑随机选择 1–20 的一个整数，玩家有 5 次机会。每次输入后，程序提示“太小”“太大”或“正确”。猜中或用完机会后结束；输入 0 可以提前退出。非整数或范围之外的输入不扣次数。最后显示游戏结果。

**Русский**

Дополните игру «Угадай число» на Java с вводом с клавиатуры. Основная структура программы уже предоставлена. Заполните **24 пропуска TODO**, чтобы реализовать инициализацию игрового объекта, подсчёт попыток, проверку догадки и вызовы методов. Рекомендуемое время: 45–60 минут.

Компьютер случайно выбирает целое число от 1 до 20. У игрока есть 5 попыток. После ввода программа сообщает: «Слишком мало», «Слишком много» или «Верно». Игра заканчивается при правильном ответе или после исчерпания попыток. Ввод 0 завершает игру досрочно. Нецелые числа и значения вне диапазона не расходуют попытки. В конце выводится результат игры.

**English**

Complete a Java guessing game with keyboard input. The program structure is provided. Fill in **24 TODO blanks** to initialize the game object, count attempts, evaluate guesses, and call methods. Suggested time: 45–60 minutes.

The computer randomly chooses an integer from 1 to 20. The player has 5 attempts. After each guess, the program displays “Too low”, “Too high”, or “Correct”. The game ends after a correct guess or when no attempts remain. Entering 0 quits early. Non-integer and out-of-range input costs no attempt. A final message reports the result.

## 2. 类与对象教学 / Классы и объекты / Understanding classes and objects

**中文**

- `GuessGame` 是描述一局游戏的类，负责保存状态和判断猜测。字段包括答案 `secret`、次数上限 `maxAttempts`、已用次数 `attempts`、胜利状态 `won`。
- 构造方法与类同名，没有返回类型，在创建对象时初始化字段。`this.secret` 表示当前对象的字段；右侧同名的 `secret` 是构造参数。
- `new GuessGame(secret, 5)` 创建一个对象，变量 `game` 保存该对象的引用。每个游戏对象都有独立的实例字段。
- `game.guess(number)` 调用对象的方法，把用户输入作为实参传入。方法更新次数，并返回 -1（偏小）、1（偏大）或 0（猜中）。
- `GuessNumberPractice` 是包含 main 的主类，负责创建对象、读取输入和显示提示。`private` 字段由游戏类内部管理，主类通过方法与游戏对象交互。

**Русский**

- `GuessGame` — класс, описывающий одну игру. Он хранит состояние и проверяет догадки. Поля: ответ `secret`, лимит `maxAttempts`, использованные попытки `attempts` и состояние победы `won`.
- Конструктор имеет то же имя, что и класс, и не указывает тип возвращаемого значения. Он инициализирует поля при создании объекта. `this.secret` — поле текущего объекта, а `secret` справа — параметр конструктора.
- `new GuessGame(secret, 5)` создаёт объект; переменная `game` хранит ссылку на него. У каждого игрового объекта собственные значения полей экземпляра.
- `game.guess(number)` вызывает метод объекта, передавая введённое число как аргумент. Метод обновляет счётчик и возвращает -1 (число слишком мало), 1 (слишком велико) или 0 (угадано).
- `GuessNumberPractice` — главный класс с методом main. Он создаёт объект, читает ввод и выводит сообщения. Поля `private` управляются внутри игрового класса; главный класс взаимодействует с объектом через методы.

**English**

- `GuessGame` describes one game. It stores state and evaluates guesses. Its fields are the answer `secret`, limit `maxAttempts`, attempts used `attempts`, and win state `won`.
- A constructor has the class name and no return type. It initializes fields during object creation. `this.secret` refers to the current object's field; `secret` on the right is the constructor parameter.
- `new GuessGame(secret, 5)` creates an object; `game` stores a reference to it. Each game object has independent instance fields.
- `game.guess(number)` calls the object's method with the user's input as an argument. The method updates the count and returns -1 (too low), 1 (too high), or 0 (correct).
- `GuessNumberPractice` contains main. It creates the object, reads input, and displays messages. Private fields are managed inside the game class; main interacts with the object through methods.

## 3. 完成步骤 / Порядок выполнения / Steps

**中文**

1. 阅读两个类，先分清字段、构造方法、普通方法和 main。
2. 按顺序填写 `__TODO01__` 到 `__TODO24__`。每个空都有中、俄、英提示。只替换标记，保留其他代码、括号和分号。
3. 先完成字段和构造方法，再完成 getter、游戏规则，最后完成对象创建、输入读取和方法调用。
4. 输入解析和异常处理框架已提供；不要删除 `INPUT>` 前缀，notebook 用它识别输入时机。未填完时不能编译是正常现象。
5. 完成后编译运行，先用固定答案 12 自测，再体验随机游戏。记录测试结果并完成解释题。

**Русский**

1. Прочитайте оба класса и найдите поля, конструктор, обычные методы и main.
2. Заполните `__TODO01__`–`__TODO24__` по порядку. Для каждого пропуска есть подсказки на китайском, русском и английском. Заменяйте только маркеры, сохраняя остальной код, скобки и точки с запятой.
3. Сначала заполните поля и конструктор, затем методы чтения состояния и правила игры, после этого — создание объектов, чтение ввода и вызовы методов.
4. Каркас преобразования ввода и обработки исключений уже предоставлен. Сохраните префикс `INPUT>`: notebook использует его для запроса ввода. До заполнения всех пропусков программа не компилируется — это ожидаемо.
5. Скомпилируйте и запустите программу. Сначала проверьте её с фиксированным ответом 12, затем сыграйте со случайным ответом. Запишите результаты и ответьте на вопросы.

**English**

1. Read both classes and identify fields, the constructor, ordinary methods, and main.
2. Complete `__TODO01__` through `__TODO24__` in order. Each blank has Chinese, Russian, and English hints. Replace only the markers; keep the other code, brackets, and semicolons.
3. Complete fields and the constructor first, then getters and game rules, followed by object creation, input reading, and method calls.
4. Input parsing and exception handling are provided. Keep `INPUT>` so the notebook can detect input requests. The unfinished program is intentionally not compilable.
5. Compile and run. Test with a fixed answer of 12 before trying random play. Record your test results and answer the questions.

### main 程序入口 / Точка входа main / The main entry point

程序已提供完整的 main 方法，无需学生填写：
Готовый метод main уже предоставлен; заполнять его не нужно:
The complete main method is provided; students do not need to fill it in:

```java
public static void main(String[] args) {
    play(args);
}
```

main 是 Java 启动程序时执行的入口，调用 play 方法进入游戏交互流程。先完成文件中的 24 个 TODO，再在 IDE 中打开 GuessNumberPractice.java，点击 main 左侧的运行按钮；输入和输出显示在控制台。不要运行 GuessGame，它没有 main。文件名须保持 GuessNumberPractice.java。

main — точка входа, которую Java выполняет при запуске. Он вызывает play для интерактивной игры. Сначала заполните все 24 TODO, затем откройте GuessNumberPractice.java в IDE и нажмите кнопку запуска рядом с main. Ввод и вывод находятся в консоли. Не запускайте GuessGame: в нём нет main. Сохраните имя файла GuessNumberPractice.java.

main is the entry point executed when Java starts the program. It calls play to run the interactive game. Complete all 24 TODOs, open GuessNumberPractice.java in your IDE, and click Run beside main. Use the console for input and output. Do not run GuessGame, which has no main. Keep the filename GuessNumberPractice.java.

### 运行方式 / Способы запуска / How to run

**Java：** 使用 JDK 8 或更高版本，在文件所在目录执行以下命令。 / Используйте JDK 8 или новее и выполните команды в папке файла. / Use JDK 8 or later and run these commands from the file's folder.

```shell
javac -encoding UTF-8 GuessNumberPractice.java
java GuessNumberPractice
```

固定答案为 12 / Фиксированный ответ 12 / Fixed answer of 12：

```shell
java GuessNumberPractice 12
```

**Notebook：** 选择 Python 3 内核，并准备 JDK。填写并运行“Java 填空”单元保存文件，再运行“开始游戏”。在输入框输入数字并按 Enter。自测时设置 `FIXED_ANSWER = "12"`；随机游戏设置为 `""`。找不到 JDK 时设置 `JDK_HOME` 为 JDK 根目录。

**Notebook:** Выберите ядро Python 3 и установите JDK. Заполните и выполните ячейку «Заполните код Java», чтобы сохранить файл, затем выполните «Начать игру». Введите число в поле ввода и нажмите Enter. Для проверки задайте `FIXED_ANSWER = "12"`, для случайной игры — `""`. Если JDK не найден, задайте `JDK_HOME` как путь к его корневой папке.

**Notebook:** Select a Python 3 kernel and have a JDK installed. Complete and run the Java exercise cell to save the file, then run “Start the game”. Enter a number in the input box and press Enter. Use `FIXED_ANSWER = "12"` for checks and `""` for random play. If the JDK is not found, set `JDK_HOME` to its root folder.

两种方式任选一种；使用 notebook 时以其中的代码为准，它会把 Java 文件保存到当前工作目录。 / Выберите один способ; при работе в notebook редактируйте код в его ячейке: она сохраняет Java-файл в текущую рабочую папку. / Choose one workflow; when using the notebook, edit its code cell, which saves the Java file in the current working directory.

## 4. 自测任务 / Самопроверка / Self-check tasks

固定答案为 12，依次输入每行给出的内容，并填写实际结果。 / Задайте ответ 12, введите значения каждой строки по очереди и запишите результат. / Fix the answer at 12, enter each sequence, and record the actual result.

| 输入 / Ввод / Input | 预期结果 / Ожидаемый результат / Expected result | 实际 / Факт / Actual |
|---|---|---|
| `8`, `16`, `12` | 偏小、偏大，第 3 次获胜。 / Мало, много, победа с 3-й попытки. / Low, high, win on attempt 3. | ______ |
| `1`, `2`, `3`, `4`, `5` | 用完 5 次，公布答案 12。 / 5 попыток исчерпаны, показан ответ 12. / Exhaust 5 attempts and reveal 12. | ______ |
| `abc`, `21`, `-1`, `12` | 无效输入不扣次数，1 次获胜。 / Неверный ввод не расходует попытки, победа за 1 попытку. / Invalid input costs no attempts; win in 1. | ______ |
| `0` | 退出，已用 0 次。 / Выход, использовано 0 попыток. / Quit with 0 attempts used. | ______ |
| `1`, `2`, `3`, `4`, `12` | 第 5 次猜中，仍然获胜。 / Верный ответ с 5-й попытки означает победу. / A correct fifth guess still wins. | ______ |

## 5. 解释与提交 / Объяснение и сдача / Explain and submit

1. 哪些字段描述一局游戏的状态？ / Какие поля описывают состояние одной игры? / Which fields describe one game's state?

   回答 / Ответ / Answer：__________

2. 在 `game.guess(number)` 中指出对象引用、方法名和实参。 / Назовите ссылку на объект, имя метода и аргумент в `game.guess(number)`. / Identify the object reference, method name, and argument in `game.guess(number)`.

   回答 / Ответ / Answer：__________

3. 两个分别创建的游戏对象会共享 attempts 吗？为什么？ / Будут ли два отдельно созданных игровых объекта совместно использовать attempts? Почему? / Do two separately created game objects share attempts? Why?

   回答 / Ответ / Answer：__________

4. 若将 `this.secret = secret;` 写成 `secret = secret;`，字段的值是多少？ / Каково значение поля, если заменить `this.secret = secret;` на `secret = secret;`? / What is the field's value if `this.secret = secret;` becomes `secret = secret;`?

   回答 / Ответ / Answer：__________

**提交要求：** 填写姓名和学号；提交完成 24 个 TODO 的 Java 文件或 notebook，以及 5 项自测记录和 4 道解释题答案。若提交 notebook，请保留运行输出。无需提交教师答案。

**Что сдать:** Укажите имя и номер студента. Сдайте Java-файл или notebook со всеми 24 заполненными TODO, результаты 5 проверок и ответы на 4 вопроса. При сдаче notebook сохраните вывод программы. Ответы преподавателя сдавать не нужно.

**Submission:** Enter your name and student ID. Submit the Java file or notebook with all 24 TODOs completed, results for the 5 checks, and answers to the 4 questions. Keep program outputs if submitting a notebook. Do not submit the teacher key.

**选做：** 增加“再玩一局”，每次重新创建游戏对象。 / **Дополнительно:** добавьте запрос новой игры, создавая новый игровой объект для каждого раунда. / **Optional:** add “Play again” and create a new game object for each round.
